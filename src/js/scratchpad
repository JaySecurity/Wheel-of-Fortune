let i = 0;
while(i < puzzleArr.length){
    word = document.createElement('div')
    while (puzzleArr[i] !== ' '){
        word.appendChild(boardLetters[i].parentNode)
        i++
    }
    board.appendChild(word)
    i++
}